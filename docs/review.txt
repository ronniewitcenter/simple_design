why set custom colors? Use the flutter's built in Theming e.g.,       theme: ThemeData(colorScheme: ColorScheme.fromSeed(seedColor: Colors.blue))
and when styling widgets use Theme.of(context) so that we can change the whole theme using one seed color though


skip these: 


1 Color Tokens
// DSColors — semantic naming, not descriptive
class DSColors {
  // Backgrounds
  static const background    = Color(0xFFFFFFFF);  // Page background
  static const surface       = Color(0xFFF5F5F5);  // Cards, containers
  static const surfaceVariant = Color(0xFFEEEEEE); // Subtle separation

  // Content
  static const onBackground  = Color(0xFF1A1A1A);  // Primary text
  static const onSurface     = Color(0xFF3D3D3D);  // Secondary text
  static const onSurfaceMuted = Color(0xFF9E9E9E); // Placeholder, captions

  // Borders
  static const outline       = Color(0xFFE0E0E0);  // Default borders
  static const outlineFocus  = Color(0xFF1A1A1A);  // Focused borders

  // Accent (single — minimal color philosophy)
  static const accent        = Color(0xFF1A1A1A);  // Primary actions
  static const accentSubtle  = Color(0xFFF0F0F0);  // Ghost button bg on hover

  // Semantic
  static const error         = Color(0xFFB00020);
  static const errorSurface  = Color(0xFFFFF0F0);
  static const success       = Color(0xFF2E7D32);
  static const successSurface = Color(0xFFF1F8E9);
  static const warning       = Color(0xFFF57C00);
  static const warningSurface = Color(0xFFFFF8E1);
  static const info          = Color(0xFF0277BD);
  static const infoSurface   = Color(0xFFE1F5FE);
}
5.2 Spacing Tokens (8pt Grid)
class DSSpacing {
  static const double xs   = 4.0;
  static const double sm   = 8.0;
  static const double md   = 16.0;  // Base unit
  static const double lg   = 24.0;
  static const double xl   = 32.0;
  static const double xxl  = 48.0;
  static const double xxxl = 64.0;
}
5.3 Typography Tokens
class DSTextStyle {
  static const displayLg = TextStyle(fontSize: 48, fontWeight: FontWeight.w700, height: 1.1);
  static const displaySm = TextStyle(fontSize: 36, fontWeight: FontWeight.w700, height: 1.2);
  static const heading1  = TextStyle(fontSize: 28, fontWeight: FontWeight.w600, height: 1.3);
  static const heading2  = TextStyle(fontSize: 22, fontWeight: FontWeight.w600, height: 1.3);
  static const heading3  = TextStyle(fontSize: 18, fontWeight: FontWeight.w500, height: 1.4);
  static const bodyLg   = TextStyle(fontSize: 16, fontWeight: FontWeight.w400, height: 1.6);
  static const body     = TextStyle(fontSize: 14, fontWeight: FontWeight.w400, height: 1.6);
  static const bodySm   = TextStyle(fontSize: 12, fontWeight: FontWeight.w400, height: 1.5);
  static const label    = TextStyle(fontSize: 14, fontWeight: FontWeight.w500, height: 1.4);
  static const caption  = TextStyle(fontSize: 12, fontWeight: FontWeight.w400, height: 1.4);
  static const code     = TextStyle(fontSize: 13, fontFamily: 'monospace', height: 1.6);
}
5.4 Radius Tokens
class DSRadius {
  static const double none   = 0.0;
  static const double xs     = 2.0;
  static const double sm     = 4.0;
  static const double md     = 8.0;   // Default
  static const double lg     = 12.0;
  static const double xl     = 16.0;
  static const double full   = 999.0; // Pills, avatars
}

5.6 Size Tokens
class DSSize {
  static const double touchTarget  = 48.0;  // Minimum (Fitts's Law)
  static const double buttonHeight = 48.0;
  static const double buttonHeightSm = 36.0;
  static const double inputHeight  = 48.0;
  static const double iconSm       = 16.0;
  static const double iconMd       = 20.0;
  static const double iconLg       = 24.0;
  static const double avatarSm     = 32.0;
  static const double avatarMd     = 40.0;
  static const double avatarLg     = 56.0;
}


keep this one 

5.5 Animation Tokens
class DSAnimation {
  static const fast   = Duration(milliseconds: 100);  // Hover, press
  static const normal = Duration(milliseconds: 200);  // Expand, collapse
  static const slow   = Duration(milliseconds: 350);  // Modals, page transitions
  static const curve  = Curves.easeInOut;
}

it is a best practice to add l10n in a design system?
if it is then let's explore it too
do we need any library for rick l10n?
the idea is that the default language is set to english
so there must some sort of centralize app strings that we can customize for all the labels and text in our UI components/widgets
and some sort of language settings to allow users to quickly change the current language
and let that l10n library to handle the translation for whatever language the user selected it will automatically translate the English to Some language e.g., transles all english strings into Korean